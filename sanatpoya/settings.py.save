








"""
Django settings for sanatpoya project.

Generated by 'django-admin startproject' using Django 5.0.2.

For more information on this file, see
https://docs.djangoproject.com/en/5.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/5.0/ref/settings/
"""

from pathlib import Path
import os
import warnings
warnings.filterwarnings('ignore', category=UserWarning, module='ckeditor')
warnings.filterwarnings('ignore', category=UserWarning, module='ckeditor_uploader')

ENV = os.getenv('DJANGO_ENV', 'local')

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-cmdkjr$(7^_7141k7!k108@2p7hnzyq%z0zegp%z5cz(o*&ffl'

# SECURITY WARNING: don't run with debug turned on in production!

if ENV == 'production':
    DEBUG = False
else:
    DEBUG = True

ALLOWED_HOSTS = ['sanatpoya.ir','www.sanatpoya.ir']
if ENV == 'production':
    ALLOWED_HOSTS = ['sanatpoya.com', '127.0.0.1', 'localhost', '89.116.22.1', 'www.sanatpoya.com']


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django_admin_listfilter_dropdown',
    'apps.main.apps.MainConfig',
    'apps.accounts.apps.AccountsConfig',
    'apps.products.apps.ProductsConfig',
    'apps.orders.apps.OrdersConfig',
    'apps.discounts.apps.DiscountsConfig',
    'apps.payments.apps.PaymentsConfig',
    'apps.warehouses.apps.WarehousesConfig',
    'apps.search.apps.SearchConfig',
    'apps.test_api.apps.TestApiConfig',
    'apps.comment_scoring_favorites.apps.CommentScoringFavoritesConfig',
    "apps.specialties.apps.SpecialtiesConfig",
    "apps.blog.apps.BlogConfig",
    "apps.advertisement.apps.AdvertisementConfig",
    "apps.chatbot.apps.ChatbotConfig",
    "apps.support.apps.SupportConfig",
    "apps.emailer.apps.EmailerConfig",
    "apps.realestate.apps.RealestateConfig",
    "apps.teams.apps.TeamsConfig",
    
    
    "django_check_seo",
    'django.contrib.sites',
    # 'cms',
    'menus',
    'treebeard',
    'channels',
    
    # 'meta'
    # 'metackeditor'
    'ckeditor',
    'ckeditor_uploader',
    'django_render_partial',
    'django_filters', 
    'django.contrib.humanize',
    "compressor",
    'django_ckeditor_5'
     
    # 'metametackeditordjango_render_partial'
    # 'rest_framework',
    
]

# ====== تنظیمات CKEditor ======
CKEDITOR_UPLOAD_PATH = "uploads/"
CKEDITOR_IMAGE_BACKEND = "pillow"

SITE_ID = 1 


MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    'middlewares.middlewares.RequestMiddleware',
]

ROOT_URLCONF = 'sanatpoya.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR,'templates/')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'apps.main.views.media_admin'
                
                 
            ],
        },
    },
]

WSGI_APPLICATION = 'sanatpoya.wsgi.application'
# تنظیم پروتکل ASGI  
ASGI_APPLICATION = 'sanatpoya.asgi.application'  
# تنظیمات channel layer  
CHANNEL_LAYERS = {  
    'default': {  
        'BACKEND': 'channels_redis.core.RedisChannelLayer',  
        'CONFIG': {  
            "hosts": [('127.0.0.1', 6379)],  
        },  
    },  
} 
# Database
# https://docs.djangoproject.com/en/5.0/ref/settings/#databases

# DATABASES = {
#    "default": {
#        "ENGINE": "django.db.backends.sqlite3",
#        "NAME": BASE_DIR / "db.sqlite3",}
# }
DATABASES = {
        'default': {
          'ENGINE': 'mysql.connector.django',
          'NAME': 'dbsanatpoya',
          'USER': 'sanatpoya',
          'PASSWORD':'$Q-F-1H9KxFH?3hQ',
          'HOST':'localhost',
          'PORT':'3306'
     }
 }

# Password validation
# https://docs.djangoproject.com/en/5.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]
# django CMS settings
# CMS_CONFIRM_VERSION4 = True

# Internationalization
# https://docs.djangoproject.com/en/5.0/topics/i18n/

# LANGUAGE_CODE = 'fa-us'

LANGUAGES = [  
    ("en", "English"),  
    ("fa", "Persian"),  
    ("fa-en", "Persian - English"),   
] 
LANGUAGE_CODE = "fa-en"

USE_I18N = True
USE_TZ = True

if ENV == 'production':
    TIME_ZONE = 'UTC'
else:
    TIME_ZONE = 'UTC'


# تنظیمات compressor
# Static files (CSS, JavaScript, Images)
COMPRESS_ENABLED = True
COMPRESS_OFFLINE = True 
STATICFILES_FINDERS = (
    'django.contrib.staticfiles.finders.FileSystemFinder',
    'django.contrib.staticfiles.finders.AppDirectoriesFinder',
    # other finders..
    'compressor.finders.CompressorFinder',
)
 


# https://docs.djangoproject.com/en/5.0/howto/static-files/

STATIC_URL = '/static/'
STATICFILES_DIRS = (os.path.join(BASE_DIR,'static/'),)
  

if ENV == 'production':
    STATIC_ROOT = '/var/www/sanatpoya/staticfiles'
else:
    STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')
COMPRESS_ROOT = STATIC_ROOT

MEDIA_URL = '/media/'
if ENV == 'production':
    MEDIA_ROOT = '/var/www/sanatpoya/media/'
else:
    MEDIA_ROOT = os.path.join(BASE_DIR,'media/')



# ====== تنظیمات رسانه (Media) ======
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
# Default primary key field type
# https://docs.djangoproject.com/en/5.0/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
AUTH_USER_MODEL    = "accounts.CustomUser"
#--------------------------------------------------------------------------
##create ckeditorبرای استفاده از ابزار های ورد در توضیحات 
CKEDITOR_UPLOAD_PATH = "images/ckeditor/upload_files/"
CKEDITOR_STORAGE_BACKEND = 'django.core.files.storage.FileSystemStorage' 
CKEDITOR_CONFIGS = {
      'default': {
        'toolbar': 'Custom',
        'toolbar_Custom': [
            ['Bold', 'Italic', 'Underline','unlink','link','image'],
            # ['NumberedList', 'BulletedList', '-', 'Outdent', 'Indent', '-', 'JustifyLeft', 'JustifyCenter', 'JustifyRight', 'JustifyBlock'],
            # ['Link', 'Unlink'],
            # ['RemoveFormat', 'Source']
        ],
    },
      'special' :
          {
              'toolbar': 'Special','height':500 ,
              'toolbar': 'full',
              'toolbar_Special':
                  [
                      ['Bold', 'Italic', 'Underline','unlink','link','image'],
                      ['CodeSnippet'],#here
                  ],'extraPlugins' :',' .join(['codesnippet','clipboard',]) #here
          },
          'special_an' :
              {
              'toolbar': 'Special','height':500 ,
              'toolbar_Special':
                  [
                      ['Bold',],
                      ['CodeSnippet',],#here
                  ],'extraPlugins' :','.join(['codessnippet',]) #here   
              }      
}
# create email info@sanatpoya.com
 
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'  
EMAIL_HOST = 'smtp.gmail.com'  # یا سرویس دیگری که شما استفاده می‌کنید  
EMAIL_PORT = 465  
EMAIL_USE_TLS = False 
EMAIL_HOST_USER = 'fariborz499@gmail.com'  # آدرس ایمیل خود را وارد کنید  
EMAIL_HOST_PASSWORD = 'inFo18657@#'  # رمز عبور خود را وارد کنید  
DEFAULT_FROM_EMAIL = 'fariborz499@gmail.com' # این خط را نیز می‌توانید اضافه کنید
x
cc
C
X
exit

